# Backend
backend/.env
backend/vendor/
backend/node_modules/
backend/storage/logs/*
backend/.phpunit.result.cache
backend/bootstrap/cache/*

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.env.local

# General
.DS_Store
Thumbs.db
*.log
.claude/

# Docker
# .env.docker is intentionally NOT ignored — it contains only placeholders
# and is safe to commit so teammates can get started without extra setup.
# Real secrets (APP_KEY, API keys) must be filled in by each developer.
docker-compose.override.yml